<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito</title>
</head>
<body>
    <h2>Bienvenido <%=user.username%> </h2>
    <img src="<%=user.url%>" alt="foto de perfil">
    <br>
    <a href="/passport/logout">Cerrar Sesión</a>

    <h3>Pedidos</h3>
    <% if(pedidos && pedidos.length !== 0){ %>
        <%- include('../partials/tablaPedidos.ejs') %>
    <%}else{%>
        <h3>No tenes nada agregado al Carrito</h3>
    <%}%>
    <br>

</body>
</html>