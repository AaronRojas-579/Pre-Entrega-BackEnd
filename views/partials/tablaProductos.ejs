<div class="tabla">
    <hr>
    <table>
        <tr>
            <th>Nombre</th>
            <th>Calidad</th>
            <th>Precio</th>
        </tr>
        <%produtosLista.forEach(per=>{%>
            <tr>
                <td><%=per.nombre%></td>
                <td><%=per.calidad%></td>
                <td><%=per.precio%></td>
                <td><button class="subirProducto" onclick="return restarCarrito(`<%=per._id%>`)">-</button> <span id="<%=per._id%>">0</span> <button class="subirProducto" onclick="return sumarCarrito(`<%=per._id%>`)">+</button></td>
                <td><button class="subirProducto" onclick="return agregarCarrito(`<%=per._id%>`)">Agregar al Carrito</button></td>
            </tr>
        <%})%>
    </table>
    <hr>
</div>